| Dear #{@order.user.first_name}:

	Your order, "#{@order.title}" has been estimated at #{number_to_currency @order.price}.

	Please log into CADsurf and pay for the order so production can begin.

	#{order_url @order}

	Thank you,

	CADsurf